import { FC } from "react";
import TweetsList from "../Components/TweetsList/TweetsList";

const Tweets: FC = () => {
  return (
    <section>
      <TweetsList />
    </section>
  );
};

export default Tweets;
